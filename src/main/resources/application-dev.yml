spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/watchdog
    username: watchdog
    password: watchdog
  exposed:
    show-sql: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    enabled: true
logging:
  level:
    i.server: debug
